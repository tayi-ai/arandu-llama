// Package native owns the bounded numerical recipe, decoder execution,
// admission contracts, checkpoint integrity and result verification of the
// supported native training backend. Applications supply explicit immutable
// configuration, authorization, process supervision, measurements and transport.
// Successful CPU fixtures do not qualify a model, GPU deployment or scientific
// method. Unsupported geometry and unqualified artifacts remain rejected.
package native
